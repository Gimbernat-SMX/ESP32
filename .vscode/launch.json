{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ejecutar ejercicio ESP32",
            "type": "node-terminal",
            "request": "launch",
            "command": "bash \"${workspaceFolder}/.vscode/run_exercise.sh\" launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}
